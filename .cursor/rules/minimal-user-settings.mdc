---
description: 少增加用户负担；避免零碎设置开关泛滥，优先产品默认与代码内策略
globs: 
alwaysApply: true
---

# 用户体验：少开关、少决策负担

## 原则

- **尽量不增加用户使用负担**：新能力优先用**合理默认行为**、清晰的一次性流程或自动推断解决，而不是把复杂度拆成大量「让用户自己选」的选项。
- **避免零碎开关泛滥**：不在设置页 / `AppConfig` / 扩展 options 里堆叠大量布尔或枚举细项；若确有必要暴露给用户，优先**合并为少量有意义的档位**（或高级折叠区），并写清默认值与适用场景。
- **产品策略放代码侧**：与协议、安全、编排强相关的规则（例如「某步之后必须先做某事」）优先用**代码内常量或单一内部开关**实现，而非每个都做成用户可配置项。

## 反例

- 为每个小行为加一个「是否启用 X」checkbox，默认值还依赖用户理解实现细节。
- 同一类需求拆成 5 个独立 toggle，文档却写「一般全开即可」。
- 把本可自动或按上下文决定的行为，改成「请用户在设置里先选对模式再回来用」。

## 正例

- 用固定产品规则 + 清晰错误提示（见 `error-visibility`）代替一排预防性开关。
- 实验性项目可破坏性调整默认行为（见 `project-experimental-no-compat`），而不是同时维护旧开关 + 新开关。
- 确需高级选项时：少数、成组、有说明、默认安全。

## 与现有规则的关系

- 与 [chrome-first-roadmap](chrome-first-roadmap.mdc) 不冲突：本条强调**不要为 AI/开发便利**把配置面铺给用户；多端对齐仍以 Chrome 为准。
