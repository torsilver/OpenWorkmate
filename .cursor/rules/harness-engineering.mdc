---
description: Agent/工具链场景下用「驾驭工程」思路：优先工程化环境与边界，而非只换模型或堆长提示词
alwaysApply: true
---

# Harness Engineering（驾驭工程）

**含义**：把 AI/Agent 的可靠性建立在**运行环境**上——上下文、边界约束、反馈与可验证性——而不是假设「换更强模型」或「写更长 system prompt」就能解决一切。效果变好是常见结果，核心是**少犯错、行为可预期、失败可诊断**。

## 三层（改代码或设计工具时对照）

1. **Context Engineering（上下文工程）**  
   - 上下文是稀缺资源：避免单文件巨型说明堆满窗口；优先**动态状态**（当前会话、工具结果、观测）与**可裁剪/可摘要**的历史。  
   - 重数据不要塞进对话：用工具落盘、引用 id、附件引用等已有模式。  
   - 提示词与工具 Description 要写清**机器可执行的形状**（分隔符、格式、何时必须调用某工具）。

2. **Architectural Constraints（架构约束）**  
   - 在**边界**收窄解空间：HTTP/API 用明确 JSON 契约与状态码；工具参数用清晰 schema；关键逻辑用单元/集成测试锁住行为。  
   - 需要模型产出结构化内容时：优先**服务端确定性解析**（例如按约定分隔符或格式拆段），而不是只靠模型自觉。  
   - 高风险路径保留 HITL、确认流或与现有安全过滤一致。

3. **新鲜度与债务（类比「过时上下文清理」）**  
   - 技能、规则文档、工具说明若与代码行为不一致，会误导 Agent；改契约时**同步** `docs/提示词清单.md`、工具 Description、相关 `.cursor/rules`。  
   - 不追求全自动「清文档机器人」；以**随功能变更更新**为主。

## 在本仓库实践时的检查清单

- 新增或修改 **Kernel 工具**：Description 是否说明输入形状、失败时是否返回**可转述的具体原因**（另见 error-visibility）。  
- 新增 **Agent 行为**：是否可用更短的静态说明 + 更稳的协议/解析，减少「模型自由发挥」。 
- 修改 **对话/子 Agent 提示**：是否与 `ContextManager`、多端 system 后缀、计划绑定注入等现有分层一致，避免重复矛盾。 
- 纯逻辑与契约：**单元测试 / 集成测试**是否覆盖新边界（见 testing 规则）。
- **压缩边界**：摘要消息勿删 `[compact_boundary:…]`；改 `ContextManager` 裁剪逻辑时保持与 `ConversationCompactBoundary` 一致（见 `docs/提示词清单.md` H 节）。
- **高危工具 HITL**：新增走 `SecurityFilter` 的任意执行类工具时，同步考虑 `AppConfig.toolPermissionRules` 与 `ToolCapabilityRegistry`（日志/元数据）。

## 反例（避免）

- 仅靠加长 system prompt 解决反复出现的同类工具调用错误。  
- 工具接受模糊长文本、服务端不做解析normalize，却把版式/结构问题完全推给模型。  
- 接口或工具失败后返回空结果、无 `message`，导致无法改进「环境」。

## 正例（与仓库一致的方向）

- 在工具侧增加**确定性分段/解析**，减轻对模型使用某个分隔符的依赖（如 Word `paragraphs` 的展开策略）。  
- 计划插件对 Markdown 步骤格式做**明确格式要求**并与解析器对齐。  
- API 错误统一 4xx/5xx + JSON `message`，前端必须展示（见 api-frontend-backend-contract）。
