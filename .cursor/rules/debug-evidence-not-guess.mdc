---
alwaysApply: true
description: 排查问题时优先用日志与可重复验证拿证据，禁止在原因不明时凭想象断定根因
---

# 调试：证据优先，禁止臆测根因

## 原则

在**原因尚未被运行证据证实**之前（日志、断点、测试失败信息、网络/响应体、复现步骤等），**不得**向用户或文档中写死「就是因为 X」这类结论，也**不得**据此大改架构或删功能。

不确定时，默认假设是：**还需要更多信息**。

## 应当怎么做

1. **加一点针对性日志**（后端见 [logging-devtools.mdc](logging-devtools.mdc)，分类名与级别要能让人过滤），再打一轮复现；或跑现有 **`dotnet test` / `wpsjs debug`** 等与问题域相关的命令。
2. **缩小范围**：二分法、最小复现、对比「好版本 / 坏版本」或「有绑定的上下文 / 无绑定」。
3. **向用户说明当前已知事实与未知点**，并明确「在拿到某条日志/某次测试结果之前只能列假设，不能当结论」。

## 反例（禁止）

- 只看现象（例如远端返回 `not found`）就长篇推断「一定是 Y 没注册」，却不查本轮请求实际带的工具表或中间件是否改写函数名。
- 不跑测试、不加日志，连续多轮改代码「碰运气」。
- 把推测当结论写进提交说明或给用户定性。

## 正例

- 「先加一条 Information/Debug 打出 A/B/C，复现后发日志再定是否刷新工具表。」
- 「假设有两条可能路径；加分支日志各打一次，下一轮即可区分。」

与 [harness-engineering.mdc](harness-engineering.mdc) 一致：**可靠性建立在可观测与可验证上，而不是换措辞解释现象。**
