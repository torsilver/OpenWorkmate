---
description: 用户附带图片与 OCR 转文档的约定
globs: 
alwaysApply: true
---

# 附件引用与图片转文档

## 附件与上下文

用户从侧栏附带图片发送时，后端会将图片存为临时文件并在对话中写入 **引用**（如 `attachment:abc123`）。历史中用户消息通常包含：「用户附带了 [attachment:xxx, attachment:yyy]。 用户说：…」。

若用户在 **Chrome 设置 → 对话大模型** 中为当前模型勾选了 **支持视觉**（`supportsVision`），同轮对话还会向模型注入 **多模态图片**（`ImageContent`），便于识别物体、场景等；**引用字符串仍会保留**，工具链不受影响。

## 处理「把图片整理成文档」时

1. **取路径**：对每个 `attachment:xxx` 调用 **File** 插件的 **get_attachment_path**，传入该引用，得到本机临时文件路径。
2. **OCR**：若已在设置中配置 **「OCR 模型」**（内置插件 **`MCP_OCR`**），调用工具 **`ocr_image`** 并传入上一步得到的路径，取得识别出的文本。密集版式、扫描件或需要高精度文字时，即使模型已看到图，仍优先用 OCR 取字再排版。
3. **组文档**：根据 OCR 返回的文本（或模型对图中文字的理解），用主模型整理、分段或生成 Markdown/Word（可配合 File、Word 等插件落盘）。

不要尝试把图片数据或 base64 直接写进用户可见的纯文本提示来「绕过」协议；多模态注入由服务端完成，文字任务通过工具链「get_attachment_path → **ocr_image**」得到文本再继续。
