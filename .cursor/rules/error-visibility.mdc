---
description: 错误必须对用户/界面可见，不得隐藏，便于排查与调试
alwaysApply: true
---

# 错误可见性：不隐藏错误

**原则**：任何失败或异常都必须以可感知的方式暴露给用户或开发者，不得用“静默失败”或笼统文案掩盖真实原因。隐藏错误会严重影响调试与问题定位。

## 必须做到

1. **API 返回**  
   - 失败时在响应 body 中返回明确的 `message`（或等价字段），说明**具体原因**（如：未配置 Embedding、API 密钥无效、参数缺失等），不要只返回 400/500 无 body 或笼统的“请求失败”。
   - 前端收到 `ok: false` 或 4xx/5xx 时，**必须把服务端返回的 message 展示给用户**（设置页、弹窗、toast 等），不要只显示空列表或“加载失败”而不写原因。

2. **工具/插件返回值**  
   - 工具（如 `save_memory`）失败时，返回给模型的字符串里要包含**可转述给用户的错误原因**（例如“Embedding 接口认证失败，请检查设置中的 API 密钥”），避免模型只回复“已保存”而实际未成功。  
   - 失败文案与前端/遥测「语义失败」判定须遵守固定前缀协议，见 [tool-result-protocol.mdc](tool-result-protocol.mdc)。

3. **日志与异常**  
   - 捕获异常后若不再抛出，必须打日志（含异常信息）；若向用户展示，展示内容应与真实错误相关，不替换为无关或模糊的提示。

## 反例（禁止）

- 接口失败却只返回空列表，不返回也不展示 `message`。
- 配置错误（如 API key 无效）时前端只显示“暂无数据”或“加载失败”，不展示服务端错误说明。
- 工具执行失败但返回成功语义的文案，导致用户误以为操作已生效。

## 正例

- GET 失败时：后端返回 `{ ok: false, message: "未配置 Embedding 模型。" }`，前端在列表区域显示该 `message`。
- 工具失败时：返回 `"[保存失败] Embedding 接口认证失败（请检查设置中的 Embedding API 密钥是否正确）。"` 供模型转述给用户。
