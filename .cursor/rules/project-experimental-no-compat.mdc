---
description: 项目为实验性质，无客户负担，做改动时无需考虑向后兼容
globs: 
alwaysApply: true
---

# 项目性质：实验性、无兼容负担

本项目为**实验性项目**，完全没有客户负担。

因此：

- **不需要考虑向后兼容**：删除或修改配置、API、数据结构时，无需保留旧字段、旧行为或迁移逻辑。
- **直接做破坏性变更即可**：例如移除某功能时，可一并删除对应配置项、前端 UI、后端接口，不必为旧配置或旧客户端做兼容处理。
