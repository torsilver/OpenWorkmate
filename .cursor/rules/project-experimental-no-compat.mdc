---
description: 项目为实验性质，无客户负担；可大胆删改，兼容与迁移非目标，Git 为唯一「回滚层」
globs: 
alwaysApply: true
---

# 项目性质：实验性、无兼容负担

本项目为**实验性项目**，完全没有外部客户、无线上 SLA、无「必须平滑升级」的约束。

## 对改动的态度

- **该删就删、该改就改**：不需要为了「少动一点」而保留半截实现、双轨逻辑或「暂时兼容」分支。能一次性删干净、改到位，优于长期背着历史包袱。
- **完全不需要考虑向后兼容**：不保留旧 JSON 键、旧 API 形态、旧配置语义、旧客户端行为；不做数据迁移脚本、不做 feature flag 长期并存、不写「读旧写新」的过渡层，除非当前任务**明确**要求（极少见）。
- **破坏性变更是常态**：移除功能时，应一并删掉相关配置、UI、接口、文档与测试中的死代码，而不是留着「万一有人还用」的兼容入口。

## 安全网

- **版本控制即回退手段**：若某次大改结果不理想，**回退到上一个 Git 提交/分支**即可；不要为了「可回滚」而在代码里堆兼容层——仓库历史本身就是回滚方案。
- Agent 与协作者不必在方案里默认预留「兼容旧版」；评审时也不应以「会破坏旧配置」为由拒绝合理简化。

## 与具体工作的关系

- 删除或修改配置、API、数据结构时，无需保留旧字段、旧行为或迁移逻辑。
- 与「错误可见性」「契约清晰」等规则**不冲突**：不兼容 ≠ 静默失败；该报错仍要报错、该写清仍要写清。
