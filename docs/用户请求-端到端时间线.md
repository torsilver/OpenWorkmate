# 用户发出一条请求后的端到端时间线

本文从**时间先后**描述：用户在侧栏发送一条消息后，数据如何流经 **Chrome 扩展 → 本机 WebSocket → 后端会话与 MAF → 再经 WebSocket 回到时间线 UI**。  
**规范实现以 Chrome 为准**（`chrome-extension/`）；Office / WPS 亦连同一套 WebSocket，细节可能略有差异。

实现锚点：`chrome-extension/sidepanel.js`（`handleSend` / `send` / `handleMessage`）、`backend/Program.cs`（`HandleSessionAsync` / `HandleChatStreamAsync`）、`backend/ChatService.cs` + `ChatService.StreamPhases.cs`、`backend/Services/Chat/Executors/ChatTurnWorkflow.cs`、`backend/Services/Maf/MafMainSessionStreamRunner.cs`。

---

## 1. 自上而下：主流程（逻辑分层）

```mermaid
flowchart TB
  subgraph U["用户层"]
    A["用户在侧栏输入并发送<br/>（可选：图片附件、绑定计划 planId）"]
  end

  subgraph CE["Chrome 扩展"]
    B["handleSend：校验连接、展示用户气泡"]
    C["send：ws.send(JSON)<br/>type=text, mode=agent, content, planId?, attachments?"]
    D["WebSocket 已连接则立即发送；否则入队 pendingMessages 并重连"]
  end

  subgraph WS["本机 Office Copilot Server · WebSocket"]
    E["HandleSessionAsync：收包、反序列化 WsMessage"]
    F["非阻塞：_ = HandleChatStreamAsync(...)"]
    G["stream_start + TimelineBlockStreamCoordinator.BeginRound"]
    H["附件：AttachmentCache → attachment: 引用写入对话"]
    I["chatService.StreamChatAsync(...) 异步枚举 StreamItem"]
  end

  subgraph CS["ChatService · 单轮"]
    J["用户消息写入会话 History<br/>（多模态或 attachment 引用）"]
    K["ChatTurnWorkflow：MAF InProcessExecution"]
    K1["ContextPrepPart1：token 预算、可选 MAF Compaction"]
    K2["ContextPrepPart2：加载计划、AI-REQUEST 日志"]
    K3["ToolingPhase：静态全量工具 或 动态工具 bootstrap<br/>（WPS 写入 WpsHostKindForTools）"]
    L["BuildContextProviders（计划/知识库等注入 MAF）"]
    M["可选：AgentGroupChat / Host 前言 / 直接主路径"]
    N["MafMainSessionStreamRunner<br/>ChatClientAgent 流式 + 工具中间件"]
    O["模型远端 API（OpenAI 兼容等）"]
    P["工具执行：插件 / MCP / RpcManager（浏览器等需扩展回包）"]
    Q["本轮助手正文落盘到 History"]
  end

  subgraph OUT["回传与收尾"]
    R["Program：StreamItem → WS 帧<br/>reasoning_chunk / stream_chunk / tool_call_delta / agent_status / agent_trace / agent_phase / stream_warning / error"]
    S["Chrome handleMessage：时间线块、流式正文、状态条"]
    T["stream_end + PersistSessionTranscriptAsync<br/>TimelineBlockStreamCoordinator.EndRound"]
  end

  A --> B --> C
  B --> D
  C --> E --> F --> G --> H --> I
  I --> J --> K
  K --> K1 --> K2 --> K3 --> L --> M --> N
  N <--> O
  N --> P
  N --> R --> S --> T
  P -.->|rpc_request / confirm_request 等| CE
  CE -.->|rpc_response / confirm_response| E
```

**要点简述**

- **为何 `HandleChatStreamAsync` 不 `await` 挂在默认分支**：工具若通过 `rpc_request` 要求扩展在同一 WebSocket 上回 `rpc_response`，若阻塞收包循环会死锁；因此聊天流在后台跑，收包循环继续服务控制类消息（见 `Program.cs` 注释）。
- **时间线上的块**：服务端 `TimelineBlockStreamCoordinator` 为 `reasoning_chunk` / `stream_chunk` 等分配 `blockSeq` / `blockKind`，前端按序渲染为「一轮」内的子块；`agent_status` / `agent_trace` 用于活动说明与结构化轨迹。
- **配置分叉**：`SemanticKernel:UseAgentGroupChatMainSession` 为真时走 `MafAgentGroupChatSessionRunner`；否则走主路径 + 可选 `UseHostPreambleAgent` + `MafMainSessionStreamRunner`。动态工具开启时，`MafMainSessionStreamRunner` 内可能多轮外层循环直至工具表稳定或达上限。

---

## 2. 同一轮对话中：WebSocket 帧的典型先后顺序（时间线视角）

以下为**常见**顺序示意；实际会因是否推理、是否调工具、是否子任务而增减。**不**表示每种请求都会出现每一类帧。

```mermaid
sequenceDiagram
  participant User as 用户
  participant SP as sidepanel.js
  participant Srv as Program.HandleChatStreamAsync
  participant Chat as ChatService
  participant LLM as 模型 API

  User->>SP: 点击发送
  SP->>Srv: text + mode + plan + attachments
  Srv-->>SP: stream_start
  Note over Chat: 上下文三阶段 + 工具准备
  Srv-->>SP: agent_status / agent_trace（多段）
  Chat->>LLM: 流式补全（多轮含 tool）
  loop 流式输出
    LLM-->>Chat: token / tool deltas
    Chat-->>Srv: StreamItem
    Srv-->>SP: reasoning_chunk 或 stream_chunk
    Srv-->>SP: tool_call_delta（参数草稿，可选）
  end
  Note over Chat,Srv: 工具执行中可能穿插 agent_phase 类轨迹、子任务 subtask_*、tool_invocation_*
  Srv-->>SP: stream_end
  Note over Srv: 持久化会话 transcript
```

更完整的 WS `type` 与前端归类，见 `chrome-extension/sidepanel.js` 中 `handleMessage` 上方注释（「已进时间线 / 未进时间线」）。

---

## 3. 只读排障时可对照的日志节点

| 阶段 | 日志或现象示例 |
|------|----------------|
| 入站 | `Program`：`WS Recv type=…`（Debug） |
| 请求体量 | `ChatService.StreamPhases`：`[AI-REQUEST] … phase=agent turns=…` |
| 动态工具 | `Dynamic tooling: bootstrapCount=…` 或 `Static tooling: …` |
| 主流结束 | `MAF 主会话流结束` |
| 一轮收尾 | `Turn completed, turns=…`；随后 `stream_end` |

若需物理拓扑（目录、端口、谁连谁），可对照 [architecture-dimensions.md](./architecture-dimensions.md)。
