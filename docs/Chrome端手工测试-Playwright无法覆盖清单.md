# Chrome 手工测试计划中 Playwright 无法（或暂不）覆盖的条目

> **与主计划同步**：最近一次与 `Program.cs`（`GET /api/tools/builtin`）、`FilePlugin`（`text_file_*`）、`SessionToolResolver` / `ClientTypeToolFilter`（动态工具 bootstrap、WPS `wpsHostKind`）对齐校准 **2026-04-15**。

本文档与 `**docs/Chrome端手工测试计划.md**` 配套：已在 `**e2e/**` 中用 Playwright 尽量覆盖「扩展壳、静态页、可选的后端 HTTP」等；**下列条目仍需你手工执行**（或将来用别的手段，如后端集成测试、带密钥的专用 E2E 环境）。

自动化用例入口：`e2e/` 目录，`npm test`（详见该目录 `package.json`）。


| 文件                                   | 覆盖摘要                                                       |
| ------------------------------------ | ---------------------------------------------------------- |
| `e2e/tests/chrome-shell.spec.ts`     | C1、C3、C4、C6、输入框 `@` 提示、options 直达                          |
| `e2e/tests/chrome-aux-pages.spec.ts` | `workspace` / `plans` / `meeting-live` / `debug-stats` 页可达 |
| `e2e/tests/backend-optional.spec.ts` | 本机后端可达时：`GET /api/tools/builtin` 返回数组 `length >= 18`、A1 `@` 列表（不可达则 **skip**） |


---

## 零、环境检查（Z1–Z5）


| 序号     | 原因                                                                    |
| ------ | --------------------------------------------------------------------- |
| **Z1** | 需本机进程常驻；Playwright 用例**不强制**启动 .NET 服务（可选用例仅在服务已启动时可跑）。               |
| **Z2** | `chrome://extensions` 版本核对属人工；Playwright 使用 **加载扩展目录** 而非与你日常浏览器完全一致。 |
| **Z3** | Token 与 `user-config` 一致、401 等需真实配置与密钥策略。                             |
| **Z4** | 模型、Embedding、百炼 ASR/OCR、`disabledBuiltInPlugins` 等依赖个人配置与密钥。          |
| **Z5** | 下载目录、相对路径与真实用户目录一致性的主观核对。                                             |


---

## 一、扩展壳与交互（§1）


| 序号          | 原因                                                                                                                              |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| **C2**      | 真机侧栏与「当前激活标签」同步依赖 Chrome Side Panel + `tabs` 焦点；测试里用标签页打开 `sidepanel.html` 时，**活动标签与侧栏不同屏**，且侧栏对标签切换的刷新逻辑与真 Side Panel 不一致，易误判。 |
| **C5**      | 依赖大模型流式输出与中途「停止」，不稳定且需可用模型与长回复。                                                                                                 |
| **V1–V3**   | 麦克风权限、百炼 ASR 真收音/拒权/密钥错误。                                                                                                       |
| **M0–M3**   | 会议监听、麦克风、实录页、下载 HTML。                                                                                                           |
| **A2**      | 选择 `@` 工具后模型是否优先调用，属 **LLM 行为**。                                                                                                |
| **P0–P1**   | 计划生成、绑定、`execute_plan_step` 等与对话/模型强耦合。                                                                                         |
| **TL1–TL7** | 时间线块类型、顺序、HITL 遮罩、`stream_warning` 等依赖 **WebSocket 帧 + 模型 + 工具执行**。                                                             |
| **WZ1–WZ2** | Workspace 打开与侧栏共存依赖模型产出 mermaid/长 Markdown 等。                                                                                   |


> **已自动化（勿写入本表）**：C1、C3、C4、C6 见 `e2e/tests/chrome-shell.spec.ts`。

---

## 二、负向（§2）


| 序号     | 原因                                                  |
| ------ | --------------------------------------------------- |
| **N1** | 需模型在 Chrome 下不误调/不误报 `CurrentDocument`，属 LLM 与对话策略。 |
| **N2** | 「调试统计或开发者手段」下工具列表核对，非固定 DOM 契约。                     |


---

## 三、内置插件与工具（§3 全部表格）

**原因（共性）**：手工文档中每条话术均依赖 **大模型按指令调用指定工具**；工具又在 **本机下载目录、Office 文件、音频、Embedding、外部 API** 等条件下执行。Playwright 当前**未**接入「可控假模型」或「直接调插件函数」的 HTTP 网关，因此 **B01–SK2、F04/F05、§3.22 DT/US 等编号用例均保留手工**（若未来为工具层写 `backend.Tests` 集成测试，可部分下沉到 .NET，仍不等价于 Chrome 侧栏 UI）。

包括但不限于：

- **3.0 数据准备**、**3.1 Browser**～**3.21 SkillAuthor**、**3.22 AgentTooling / UserSkillProgressive** 各表全部行；
- **3.2 File** 中 **`text_file_read` / `text_file_write`**（F04/F05）；
- **3.2a Pdf 负向**、**3.10 读工具接地** 等说明性场景；
- **WS1**（百炼 `enable_search` 联网）；
- **ST6**（cron 与时区）等可选项。

---

## 四、选项页与配置（§4）


| 序号     | 原因                                                                 |
| ------ | ------------------------------------------------------------------ |
| **O1** | 「运行时 ToolRegistry 与 **`GET /api/tools/builtin`（当前固定 18 条、缺 Pdf 等 5 项）** 差异、`disabledBuiltInPlugins` 逐项停用」需人工对照与改配置重载。 |
| **O2** | 改模型/密钥/目录并验证侧栏重连，涉及真实密钥与保存流程。                                      |
| **O3** | 外部 MCP 工具列表因环境而异。                                                  |
| **O4** | Skills 启用/禁用后 `@` 列表变化，需改配置并目视。                                    |


> Playwright 仅做 **options.html 能打开** 的壳层检查（与 C3 重叠）；深度配置仍手工。

---

## 五、调试与可观测（§5）


| 序号     | 原因                          |
| ------ | --------------------------- |
| **D2** | 后端控制台/日志文件人工查看。             |
| **D3** | 时间线与 WS 发帧顺序对照，需同时看后端日志与侧栏。 |


> **D1**（debug-stats 页不白屏）可在 `e2e/tests/chrome-aux-pages.spec.ts` 做浅层断言；统计数值正确性仍建议人工扫一眼。

---

## 六、§六 覆盖核对表

整表为**清单式核对**，建议在完成 §3 手工与工具命中率统计时使用；Playwright 不替代该表。

---

## 七、记录模板（§七）

测试记录与「工具调用命中率」统计保留手工填写。

---

## 小结


| 类别            | 建议                                                              |
| ------------- | --------------------------------------------------------------- |
| **每次发版前**     | 在 `e2e/` 执行 `npm ci`；若本机尚未拉 Playwright 浏览器，先 `npx playwright install`；再 `npm test`。通过后按 `Chrome端手工测试计划.md` 做 §3 等手工。 |
| **想减少 §3 手工** | 在 `backend.Tests` 为各 `ToolFunction` 增加集成测试（无 UI），与 Chrome 手工互补。 |
